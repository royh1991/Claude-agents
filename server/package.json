{
  "name": "gantry-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Gantry console server: backend-repo catalog reader, agent.yaml authoring/publish, Airflow run proxy.",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "yaml": "^2.9.0"
  }
}
